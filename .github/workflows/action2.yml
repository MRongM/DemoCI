name: Action2
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Demo-job:
    runs-on: python-3.8
    steps:
      - name: pip install
        run: pip install -r requirements.txt
      - name: pytest
        run: pytest
      - name: python
        run: python app.py
